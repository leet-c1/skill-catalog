# CLAUDE.md

This workspace is for querying business data from SaaS tools like Salesforce via ConductorOne-managed MCP access.

## Demo Reset

Before each demo run:
```bash
rm -f /tmp/salesforce-access-granted
```
